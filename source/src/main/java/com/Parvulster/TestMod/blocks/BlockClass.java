package com.Parvulster.TestMod.blocks;

import net.minecraft.block.Block;
import net.minecraft.block.material.Material;

public class BlockClass extends Block{ //Undoing private class data, required (I think) if using blocks
	public BlockClass(Material materialIn) {
		super(materialIn);
	}
	
}
